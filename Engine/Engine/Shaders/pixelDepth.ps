Texture2D Texture;
SamplerState Sampler;

struct PixelInputType
{
    float4 position : SV_POSITION;
	float2 tex : TEXCOORD0;
};

void Main(PixelInputType input)
{  	
	if (Texture.Sample(Sampler,input.tex).a < 0.7)
		discard;	   	    
}